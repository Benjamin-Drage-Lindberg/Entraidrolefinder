// Import the App component
// In a real setup, you would use modules, but for simplicity we'll use script tags

// This file serves as the main entry point for the application
// The actual app logic is in components/App.js and will be loaded via script tag

console.log('Entra ID Role Finder - Frontend loaded successfully');

// Set default theme to dark mode
document.documentElement.setAttribute('data-theme', 'dark');
